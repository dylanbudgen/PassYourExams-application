<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dylanbudgen.PassYourExams">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name="dylanbudgen.PassYourExams.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="dylanbudgen.PassYourExams.Activities.QuestionsActivity" />
        <activity android:name="dylanbudgen.PassYourExams.Activities.AdvertActivity" />
        <activity android:name="dylanbudgen.PassYourExams.Activities.EndGameActivity" />
        <activity android:name="dylanbudgen.PassYourExams.Activities.LessonGridActivity"/>
    </application>

</manifest>
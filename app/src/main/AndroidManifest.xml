<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="***REMOVED***PassYourExams">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name="***REMOVED***PassYourExams.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="***REMOVED***PassYourExams.Activities.QuestionsActivity" />
        <activity android:name="***REMOVED***PassYourExams.Activities.AdvertActivity" />
        <activity android:name="***REMOVED***PassYourExams.Activities.EndGameActivity" />
        <activity android:name="***REMOVED***PassYourExams.Activities.LessonGridActivity"/>
    </application>

</manifest>